import { SearchProductOrder } from './search-product-order';

describe('SearchProductOrder', () => {
  it('should create an instance', () => {
    expect(new SearchProductOrder()).toBeTruthy();
  });
});
