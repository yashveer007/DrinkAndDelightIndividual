import { SearchRawMaterialOrder } from './search-raw-material-order';

describe('SearchRawMaterialOrder', () => {
  it('should create an instance', () => {
    expect(new SearchRawMaterialOrder()).toBeTruthy();
  });
});
