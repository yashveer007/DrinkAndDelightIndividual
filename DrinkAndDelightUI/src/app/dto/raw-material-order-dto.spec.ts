import { RawMaterialOrderDto } from './raw-material-order-dto';

describe('RawMaterialOrderDto', () => {
  it('should create an instance', () => {
    expect(new RawMaterialOrderDto()).toBeTruthy();
  });
});
